// External scanners for grammars whose SwiftPM manifests drop src/scanner.c under
// sandboxed manifest evaluation (JavaScript, Python, YAML, CSS). Kept in sync with
// the exact grammar versions pinned in Package.swift; `make sync-scanners` refreshes them.
